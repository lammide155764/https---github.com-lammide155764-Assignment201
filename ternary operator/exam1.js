const age = 18;

// Using ternary operator to check if someone is an adult
const isAdult = age >= 18 ? 'Yes, you are an adult.' : 'No, you are not an adult.';

console.log(isAdult); // Output: Yes, you are an adult.
