// Example of object destructuring
const person = {
    name: 'Lami',
    age: 30,
    city: 'New York'
};

// Extracting properties into variables
const { name, age } = person;

console.log(name); // Output: Lami
console.log(age);  // Output: 30
